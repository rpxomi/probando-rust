/*
rustup doc std::fmt
cargo new <nombre-del-proyecto>
cargo check
cargo build
cargo build --release
cargo run
cargo run --release
*/

fn main() {
    println!( "¡Hola 🌍! Soy Super Juan y estoy probando Rust." );
}
